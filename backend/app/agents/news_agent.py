class NewsAgent:

    def fetch_news(self, ticker):

        return [
            f"{ticker} shows strong growth",
            f"{ticker} market outlook bullish",
            f"Analysts optimistic about {ticker}"
        ]